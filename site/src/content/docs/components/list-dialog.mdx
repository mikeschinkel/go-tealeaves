---
title: "List Dialog"
description: Generic editable list dialog with cursor navigation, inline editing, and CRUD operations.
---

import { Aside } from '@astrojs/starlight/components';

> **Module:** `github.com/mikeschinkel/go-tealeaves/teamodal` · **Type:** `ListModel[T]`

## Install

```bash
go get github.com/mikeschinkel/go-tealeaves/teamodal
```

Generic editable list dialog with cursor navigation, inline editing, and CRUD operations. The type parameter `T` must implement the `ListItem` interface (`ID()`, `Label()`, `IsActive()`). Includes a built-in help visor toggled with `?`.

## Quick Example

{/* verified: site/examples/list-dialog/ — go test passes */}
```go
modal := teamodal.NewListModel[MyItem](items, &teamodal.ListModelArgs{
    Title:      "Select a Profile",
    MaxVisible: 8,
    LabelWidth: 20,
})

// Open it
m.modal = m.modal.Open()

// In View()
if m.modal.IsOpen() {
    return m.modal.OverlayModal(backgroundView)
}

// Handle responses
case teamodal.ItemSelectedMsg[MyItem]:
    // Space pressed — preview-select without closing
case teamodal.ListAcceptedMsg[MyItem]:
    // Enter pressed — accept selection and close
case teamodal.ListCancelledMsg:
    // Esc pressed — close without selection
case teamodal.NewItemRequestedMsg:
    // 'a' pressed — request new item creation
case teamodal.DeleteItemRequestedMsg[MyItem]:
    // 'd' pressed — contains Item to delete
case teamodal.EditCompletedMsg[MyItem]:
    // Inline edit completed — contains Item and NewLabel
```

## Features

- Generic `ListModel[T]` with type-safe item handling
- Inline editing with overwrite-on-first-keystroke behavior
- Scrollable list viewport with scrollbar indicator
- Built-in help visor overlay for discoverability (toggle with `?`)
- CRUD operations: add (`a`), edit (`e`/`F2`), delete (`d`)
- ANSI-aware overlay compositing via `OverlayModal()`
- Non-nil `tea.Cmd` message consumption signal (see [Modal Message Consumption](/go-tealeaves/guides/modal-consumption/))
- Auto-centering on screen based on `SetSize()` dimensions

## API Reference

### Constructor

| Function | Signature | Description |
|----------|-----------|-------------|
| `NewListModel` | `NewListModel[T ListItem](items []T, args *ListModelArgs) ListModel[T]` | Create an editable list dialog |

### Key Types

| Type | Description |
|------|-------------|
| `ListModel[T]` | Generic editable list dialog — `T` must implement `ListItem` |
| `ListItem` | Interface: `ID() string`, `Label() string`, `IsActive() bool` |
| `ListModelArgs` | Configuration struct — see fields below |

### ListKeyMap Fields

| Field | Default keys | Description |
|-------|-------------|-------------|
| `Up` | `up`, `k` | Move cursor up |
| `Down` | `down`, `j` | Move cursor down |
| `Accept` | `enter` | Accept selection and close |
| `Cancel` | `esc` | Cancel and close without selection |
| `New` | `a` | Request new item creation |
| `Edit` | `e`, `F2` | Begin inline editing |
| `Delete` | `d` | Request item deletion |
| `Preview` | `space` | Preview-select (mark active, stay open) |
| `Help` | `?` | Toggle help visor |

### ListModelArgs fields

| Field | Type | Description |
|-------|------|-------------|
| `Title` | `string` | Modal title |
| `MaxVisible` | `int` | Maximum visible items (default: 8) |
| `LabelWidth` | `int` | Fixed label width; 0 = auto from max item label |
| `ScreenWidth` | `int` | Screen width for centering |
| `ScreenHeight` | `int` | Screen height for centering |
| `BorderStyle` | `lipgloss.Style` | Optional border style override |
| `TitleStyle` | `lipgloss.Style` | Optional title style override |
| `ItemStyle` | `lipgloss.Style` | Optional item style override |
| `SelectedItemStyle` | `lipgloss.Style` | Optional selected-item style override |
| `ActiveItemStyle` | `lipgloss.Style` | Optional active-item style override |
| `FooterStyle` | `lipgloss.Style` | Optional footer style override |

### ListModel public fields

| Field | Type | Description |
|-------|------|-------------|
| `Keys` | `ListKeyMap` | Key bindings (can be replaced before `Open()`) |
| `Logger` | `*slog.Logger` | Optional structured logger for debug output |

### Theming

| Method | Signature | Description |
|--------|-----------|-------------|
| `WithTheme` | `WithTheme(theme teautils.Theme) ListModel[T]` | Apply all list styles from a `teautils.Theme`; sets `BorderStyle`, `TitleStyle`, and the list-specific styles `ItemStyle`, `SelectedItemStyle`, `ActiveItemStyle`, `FooterStyle`, `StatusStyle`, and `EditItemStyle` |

### Messages

| Message | When sent |
|---------|-----------|
| `ItemSelectedMsg[T]` | Space pressed in list — preview-select without closing |
| `ListAcceptedMsg[T]` | Enter pressed in list — accept selection and close |
| `ListCancelledMsg` | Esc pressed in list — close without selection |
| `NewItemRequestedMsg` | `a` pressed in list — request new item creation |
| `DeleteItemRequestedMsg[T]` | `d` pressed in list — contains `Item` to delete |
| `EditCompletedMsg[T]` | Inline edit completed — contains `Item` and `NewLabel` |

## Configuration

`ListModel` provides both wither methods and setter methods (`SetSize`, `SetItems`, `SetTitle`, etc.).
`WithTheme()` accepts a `teautils.Theme` and applies `theme.Modal.*` and `theme.List.*` style fields. Individual `With*Style()` calls made after `WithTheme()` take precedence.

## Keyboard Shortcuts

| Key | Action |
|-----|--------|
| `Up` / `k` | Move cursor up |
| `Down` / `j` | Move cursor down |
| `Space` | Preview-select (mark active, stay open) |
| `Enter` | Accept selection and close |
| `a` | Add new item |
| `e` / `F2` | Edit item inline |
| `d` | Delete item |
| `?` | Toggle help visor |
| `Esc` | Cancel and close |

## Known Limitations

<Aside type="caution">
- Modal width is determined by content — there is no explicit width control.
- Requires screen size to be set via `SetSize()` for correct centering.
- Mouse click support requires `tea.WithMouseCellMotion()` on the `tea.Program`.
</Aside>

### Error Variables

| Variable | Description |
|----------|-------------|
| `ErrModal` | Sentinel error for the `teamodal` package |
| `ErrCancelled` | Returned when the modal is cancelled |
| `ErrInvalidBounds` | Returned on invalid position bounds |

## Related Components

- [Confirm Dialog](/go-tealeaves/components/confirm-dialog/) — Simple Yes/No and OK dialogs
- [Choice Dialog](/go-tealeaves/components/choice-dialog/) — Multi-option selection dialog with hotkeys
- [MultiSelect Dialog](/go-tealeaves/components/multiselect-dialog/) — Checkbox list modal for selecting multiple items at once
- [Help Visor](/go-tealeaves/components/help-visor/) — The help overlay toggled with `?` inside the list modal
